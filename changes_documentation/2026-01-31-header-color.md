# 2026-01-31 – Flyer header color update

- Updated the flyer header background to `#1D0083` in `frontend/src/styles/global.css` to align with the requested color while retaining existing layout and typography.
- Set primary button background to `#F6CC00` in `frontend/src/styles/global.css` to match the requested button color while keeping hover/active states intact.
- Added dedicated CSS variables `--royal-purple` and `--battle-gold` to the global palette so the two requested brand colors are defined in one place and applied to the header and buttons.
- Aligned the broader UI to the two-color palette by mapping `--paper-cream` to `#F6CC00` and setting card backgrounds to the battle gold, keeping ink-black borders for contrast.
