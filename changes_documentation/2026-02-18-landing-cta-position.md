# 2026-02-18 - Landing CTA vertical position tweak

- Moved the `Enter the Arena` button lower on the landing page so the glove artwork is not visually crowded.
- Implemented by adding a responsive top margin to `.landing-cta` in `frontend/src/styles/global.css`.
- Tuned the margin slightly larger after visual review to push the CTA a bit lower.
- No behavior changes, only layout adjustment for better visual balance.
